name: http_requests
description:  HTTP Requests Package Inspired By Python Requests Module Which Is Used For To Make Http Request And Get Response You Can Use It In Rest Api
version: 0.0.1
homepage: https://github.com/secanonm/http_requests
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  crypto: ^3.0.1
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
